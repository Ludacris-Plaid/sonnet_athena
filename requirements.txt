# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Data / DB ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2

# --- LLM / embeddings ---
httpx==0.27.2
sentence-transformers==3.1.1
faiss-cpu==1.8.0.post1
numpy==1.26.4

# --- Auth (Supabase) ---
# passlib/python-jose removed — Supabase Auth owns credentials and token
# issuance now; the backend only verifies tokens (PyJWT) and, for the
# one-time admin bootstrap script, calls the Supabase admin API.
pyjwt[crypto]==2.9.0
supabase==2.9.0

# --- CLI ---
typer==0.12.5
rich==13.8.1

# --- Utilities ---
python-dotenv==1.0.1
tenacity==9.0.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Telephony (voice) ---
twilio==9.3.2

# --- Document parsing (Documents tab) ---
pypdf==5.0.1
python-docx==1.1.2

# --- CRM credential encryption ---
cryptography==43.0.1
edge-tts==7.2.8
faster-whisper==1.2.1
